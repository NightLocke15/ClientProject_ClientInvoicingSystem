import 'package:flutter/material.dart';

class SetupThree extends StatelessWidget {
  final VoidCallback onPressed;

  const SetupThree({super.key, required this.onPressed});

  @override
  Widget build(BuildContext context) {
    return Center(

    );
  }  
}