import 'package:flutter/material.dart';

class SetupTwo extends StatelessWidget {
  final VoidCallback onPressed;

  const SetupTwo({super.key, required this.onPressed});

  @override
  Widget build(BuildContext context) {
    return Center(

    );
  }  
}